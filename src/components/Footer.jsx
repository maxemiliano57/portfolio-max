const Footer = () => {
    return (
        <footer className="bg-black text-white py-6 mt-16 text-center">
            <p className="text-sm opacity-70">&copy; {new Date().getFullYear()} Max Rivera. Designed with ❤️ and Tailwind CSS.</p>
        </footer>
    );
};

export default Footer;
